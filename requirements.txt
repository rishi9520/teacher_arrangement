# Core dependencies
streamlit>=1.31.0
pandas>=2.0.0
numpy>=1.24.0

# UI components
streamlit-lottie>=0.0.5

# File handling
pathlib>=1.0.1

# Authentication and data management
# Note: 'auth.py' and 'data_manager.py' are custom modules in your project
# Their dependencies are included here as well

# For sending OTP (if using Twilio in auth.py)
twilio>=8.0.0

# UI theming and styling
# Note: utils/theme.py is likely a custom module

# For JSON handling (used in load_lottie_file)
json>=2.0.9

# Firebase integration (commented out in your code but might be needed)
# firebase-admin>=6.2.0

# For regex operations
re>=2.2.1

# HTML component support
streamlit.components.v1

# Additional dependencies for charting and visualizations
# (if used in dashboard, reports, etc.)
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.18.0

# Date/time handling (likely used in scheduling components)
python-dateutil>=2.8.2

# Excel file support (if needed for data import/export)
openpyxl>=3.1.2
xlsxwriter>=3.1.0

# CSV handling
csv>=1.0

# For potential email notifications
# smtplib (built-in)
